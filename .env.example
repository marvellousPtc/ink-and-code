###
 # :file description: 
 # :name: /ink-and-code/.env.example
 # :author: PTC
 # :copyright: (c) 2026, Tungee
 # :date created: 2026-02-01 22:41:43
 # :last editor: PTC
 # :date last edited: 2026-02-02 21:57:18
### 
# ===========================================
# 数据库配置
# ===========================================
DATABASE_URL=postgresql://username:password@localhost:5432/ink_and_code

# ===========================================
# NextAuth 认证配置
# ===========================================
# 用于加密 session 的密钥（必填）
# 生成方式: openssl rand -base64 32
AUTH_SECRET=your-secret-key-here

# 生产环境必须设置为你的实际域名（必填）
AUTH_URL=http://your-domain.com:3000

# 信任主机（可选，设置 AUTH_URL 后可不设置）
# AUTH_TRUST_HOST=true

# ===========================================
# GitHub OAuth 配置
# ===========================================
# 在 https://github.com/settings/developers 创建 OAuth App
# Authorization callback URL 设置为: {AUTH_URL}/api/auth/callback/github
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret

# ===========================================
# Google OAuth 配置（可选）
# ===========================================
# 在 https://console.cloud.google.com/apis/credentials 创建 OAuth 2.0 客户端
# 授权重定向 URI 设置为: {AUTH_URL}/api/auth/callback/google
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# ===========================================
# AI 助手配置（可选）
# ===========================================
DEEPSEEK_API_KEY=your-deepseek-api-key

# ===========================================
# Giscus 评论配置（可选）
# ===========================================
# 在 https://giscus.app 获取配置
NEXT_PUBLIC_GISCUS_REPO=your-username/your-repo
NEXT_PUBLIC_GISCUS_REPO_ID=your-repo-id
NEXT_PUBLIC_GISCUS_CATEGORY=Announcements
NEXT_PUBLIC_GISCUS_CATEGORY_ID=your-category-id

# ===========================================
# 默认图床配置（可选）
# ===========================================
# 为未配置自己图床的用户提供默认上传服务
# 每个用户限制：50MB 或 100 张图片
DEFAULT_OSS_REGION=oss-cn-hangzhou
DEFAULT_OSS_BUCKET=your-default-bucket
DEFAULT_OSS_ACCESS_KEY_ID=your-access-key-id
DEFAULT_OSS_ACCESS_KEY_SECRET=your-access-key-secret
DEFAULT_OSS_DIR=public
DEFAULT_OSS_DOMAIN=https://cdn.your-domain.com

DEVELOPER_TOKEN=ink_b8729fd60e6c7a499aed138b8ef5dab64f332f9491964131
